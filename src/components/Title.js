import React from 'react'

function Title({title}) {
    return (
        <div className="secttion-title">
            <h1>{title}</h1>
            <div></div>
        </div>
    )
}

export default Title